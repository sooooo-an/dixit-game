describe('useGameStore Zustand Store', () => {
  beforeEach(() => {
    console.log('test success')
    // useGameStore.setState(initializeGame) // 초기 상태로 리셋
  })

  test('test', () => {
    expect('test').toBe('test')
  })
})
